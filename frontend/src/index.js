import React from 'react';
import ReactDOM from 'react-dom';
import App from './app'

ReactDOM.render(
  /* Call app and show it in root id of index.html */
  <App/>, document.querySelector('#root')
);